import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lushlifelikenightflight.storeregilog",
  appName: "StoreRegiLog+",
  webDir: "dist",
};

export default config;
